/**
 * Data structure of all possible tag & class references required 
 * to componetize the the Leadership Principles Section.
 */
export enum MY_PRINCIPLES_ELEMENTS {
    RULES_ITEM = '.rules-item',
    SVGS = '.rules-item svg',
    SVG_NUMBER = '.rules-item svg text',
    HEADLINE_TEXT = '.rules-item .rules-item-headline span',
    COPY_TEXT = '.rules-item .rules-item-copy',
  }